# 更新日志

本文件记录面向使用者和维护者的重要变更；细粒度开发历史保留在 Git 提交中。

## 0.4.0 — 2026-09-23

### 新增

- 地图浏览改为按 Everest 实际生效的 Campaign 组织地图，支持原版地图、跨 Mod 覆盖，以及 A/B/C Side 切换。
- 识别 CollabUtils2 大厅并按日志引用展开地图；新增“隐藏地图集”，集中浏览投稿目录、训练场和被 Helper 隐藏的地图。
- 支持在 TUI 中编辑大厅展开的 Campaign。日志扫描结果作为初始值，本地配置可覆盖共享配置，并按大厅 SID 与 Side 独立保存。
- 地图预览支持跨地图入口、历史前进与后退、房间定位、初见路线与用时叠加；右键实体或 Trigger 可查看属性并执行入口跳转或收集品屏蔽。
- 路线编辑支持每个房间的实际房间数，地图预览与路线编辑共享保存、取消及未保存修改保护。
- 新增声明式地图隐藏规则，按已加载 Helper 及其设置分流测试地图，同时保留可见诊断。

### 改进与修复

- 按已加载内容的 crawl 顺序合并多语言 Dialog，使地图名、Campaign 名、作者与合集标签遵循实际覆盖结果。
- 修复切换存档、Campaign 集合、大厅展开和地图 Side 后的选中状态、排序与详情刷新。
- 收紧 Content 路径、SID、Dialog key、Mod 名称和依赖版本的比较语义；跳过无效 ZIP 条目和无法解析的 Mod metadata，而不阻断其余地图。
- 运行时数据统一写入 `.pist`：源码工作区使用项目目录，安装包使用平台用户数据目录，并支持 `PIST_DATA_DIR` 覆盖。
- 改善地图右键菜单和浏览器属性浮窗；属性窗会保持在可视区域内，并支持中键拖动。

### 性能

- 大厅日志引用和日志图标顺序按 Mod 内容指纹持久化缓存，只在来源变化后重新解析。
- Collab 地图列表按需加载并显示进度，避免浏览器启动时解析全部地图；缓存命中时直接复用排序结果。
- 复用 ZIP 内容索引和打开状态，减少地图浏览期间的重复归档遍历与路径分配。

### 重构与维护

- 建立 `ContentPath`、Game/Mod 内容源、Level + Side、Campaign 与 Lobby 的领域模型，统一地图身份和资源访问。
- 将地图浏览器按 Campaign 列表、地图列表、Collab 展开、排序协调、记录编辑和地图动作拆分为职责明确的模块。
- 增加路径与外部标识比较语义文档，并强化大厅配置、地图隐藏设置和浏览器协议的边界校验。

## 0.3.1 — 2026-09-20

- 更正 README 中的待办标题。

## 0.3.0 — 2026-09-20

### 新增

- 根据游戏存档中的已收集实体 ID 核对地图实体规则；遇到未匹配、明确排除或尚未审查的属性变体时，会阻止记录写入并提供刷新规则入口。
- 根据单次通关、普通通关、当前会话、用时及可收集物完成度推断地图进度，并在浏览与草稿中显示结果。
- 实体审计支持在规则刷新时按实体展示 TOML 差异，并更早显示已知的实体摘要。

### 改进与修复

- 支持读取一份 `everest.yaml` 中的多条 Mod metadata：浏览器仍按包显示首条 metadata 名称，同时可解析其余条目的依赖与禁用状态。
- 对齐 Everest 的 Mod 候选筛选和路径大小写语义，并支持 `everest.yml` 作为 `everest.yaml` 的回退清单名。
- Mod 浏览树按 Mod 元数据名进行不区分大小写排序；地图无 Dialog 时使用完整地图路径生成 fallback 名称。
- 修复地图有效游玩记录判定、同一房间多个属性变体的合并计数，以及实体规则刷新时冗余默认规则的生成。

### 性能

- 实体审计改为延迟计算变体数、按需加载详情和出现位置，并用虚拟列表、SQLite 聚合和会话内排序缓存降低大型审计库的交互延迟。
- 优化 ZIP Mod 路径索引与合集日志图标缓存，减少 Mod 浏览启动和合集排序的重复 I/O。

### 重构与维护

- 按领域与用户工作流重组实体审计、Mod 浏览和地图预览代码；收紧 Pydantic 边界模型、地图预览请求协议与存档时长解析。
- 删除内部开发阶段已升级的本地数据兼容层，归档当前不参与产品工作流的 Loenn 静态分析子系统。
- 增加项目开发与审查技能，明确外部输入校验、模块边界、性能检查和测试原则。

## 0.2.0 — 2026-09-17

- 发布 `pist-0.2.0` 压缩版本。
